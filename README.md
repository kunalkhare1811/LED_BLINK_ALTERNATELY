# LED_BLINK_ALTERNATELY
This project demonstrates how to blink multiple LEDs alternately using a microcontroller such as an Arduino. While one LED turns ON, the other turns OFF, creating an alternating blinking pattern.
